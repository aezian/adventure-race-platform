apiVersion: v1
kind: Service
metadata:
  name: adventure-race-api-service
spec:
  type: NodePort
  selector:
    app: adventure-race-api
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080
